# Журнал харнеса

Что мешало Claude работать по правилам, что поменяли в `CLAUDE.md`, скиллах и агентах, замечания
Милы о том, как с ней разговаривать. Только дописывать в конец. Правила и скиллы меняются на
основании записей отсюда, а не по памяти.

Формат: `## ГГГГ-ММ-ДД · короткое имя` → **Что было** (словами Милы или наблюдение Claude) →
**Что сделали** (файл и суть правки, или «пока ничего — предложение Диме»).

---

## 2026-09-16 · харнес v1 собран

**Что было.** Правила жили в `CLAUDE.md`, точек входа для Милы не было, приёмочный тест
требовал конвейер forge, который владелец отверг 2026-09-03.

**Что сделали.** `CLAUDE.md` переписан под четыре маршрута; `.claude/settings.json` (русский
язык ответов, запреты на разрушительные команды); восемь скиллов; шесть агентов-критиков вместо
`forge-*`; папка `brain/`; стенд с демо-данными и живым режимом; приёмочные тесты харнеса и
языка кода. Спецификация — `brain/specs/2026-09-16-harness.html`.

## 2026-09-16 · два ревью харнеса до первой сессии Милы

**Что было.** Ревью гайдов и скиллов (агент-редактор) и ревью кода (агент-критик) над коммитом
1715df2. Крупное: после приватизации репозитория `git clone` по HTTPS спросит пароль — в
`SETUP-RU` не было `gh auth login`; флаг `disable-model-invocation` на `/relize` не давал
подхватить скилл по слову «выкатываем»; `CLAUDE.md` запрещал вливать в `master` вне `/relize`,
а `/avaria` это делает; число подтверждений Милы расходилось между четырьмя документами;
`scripts/rc.sh` отбивал `down -v`, но пропускал `down --volumes` и любой SQL через `psql`;
`prod-snapshot.sh` тянул сырые строки прод-логов (номера и тексты участниц) в `brain/ops/` под
git; демо-отчёты получали время «сегодня вечером» и уезжали в будущее; макет переписки терял
догоняющие сообщения; `_clean` пропускал атрибуты тегов; `dev-stack.sh down` удалял базу стенда.

**Что сделали.** `SETUP-RU` §3 — `gh auth login` и `gh repo clone`; `/relize` — флаг снят,
триггеры в `description`; `CLAUDE.md` «Branches» — `/relize` или `/avaria`; три слова Милы за
фичу (план, «сливаем», «выкатываем») и одно за микро-правку — одинаково в `CLAUDE.md`,
`GUIDE-RU`, `brain/README.md`, спеке; `rc.sh` — белый список подкоманд и только `select` без `;`
для `psql`; `prod-snapshot.sh` — счётчики и классы ошибок, сырые строки в `.dev/prod-logs/`,
токен вычищается; `demo_data._at` не позже «сейчас»; `chat_mockup` — один курсор и дедуп,
теги пересобираются без атрибутов; стенд: `down` останавливает, `reset` удаляет, `restart`
помнит режим, `link admin`. Запрет `Bash(rm -rf *)` из `settings.json` проверен через
`claude -p`: команда отклонена. Все тесты приёмки расширены на эти случаи.

**Спорное, оставлено как есть.** Зеркало бэкапов на маке с `--delete` (это копия 30-дневного
окна сервера, а не архив навсегда — честно описано в `SETUP-RU` §7). `libpq` на маке нужен:
`test_stage6_ops` запускает `pg_dump` локально.

## 2026-09-16 · сухой прогон «Claude Милы получает первое задание»

**Что было.** Агент сыграл Claude, которому Мила написала две просьбы в одном сообщении
(дата присоединения в паспорте; точка в приветствии бота), и шёл строго по скиллам от
`/status` до плана. Маршруты («фича» и «микро-правка») назвал верно, три вопроса Миле были по
делу, а по дороге нашёл настоящий продуктовый факт: `/api/home` не отдаёт дату присоединения,
и «с начала сезона» неоднозначно (сезон с 18.08, первая неделя с 31.08). Что мешало:
`/zadacha` велел `git switch dev && git pull --ff-only`, а у `dev` нет upstream; `shots.sh`
перетирал снимок одной участницы снимком другой (имя файла — только по странице); `/otchet`
требовал агента-редактора и не давал обходного пути там, где агентов нет; приёмка «4–7
пунктов» тесна; `brain_index.py` ставил ссылку на обещанный, но не существующий `plan.html`;
`/status` молчал, когда снимков прода нет вовсе; и главный — макет переписки показывал
«Штампов: 45 из 12» вместо «2 из 12»: разделители `\x00N\x00` в `_clean` складывались из
хвоста одного ключа, цифры текста и головы следующего.

**Что сделали.** `/zadacha`: ветка от `dev`, если она есть, иначе от `master`, папка задачи —
после переключения; кем снимать «было» и что `--as` даёт суффикс файла; приёмка 4–8.
`scripts/shots.sh --as` — суффикс `-<id>`. `/otchet`: без агента — проверка самой по рубрике
`editor-report` с записью в журнал; открывать каждый скриншот и макет глазами до вставки.
`brain_index.py`: ссылка только на существующий файл, иначе «обещан, файла нет». `/status`:
что говорить без снимков. `chat_mockup._clean`: разделители `\x00N\x01` и одна проходка
регулярным выражением; регрессионный тест с цифрами между тегами. Карточки учебных задач
02/03 из репозитория убраны — это был прогон, а не работа Милы; баг макета остаётся в
`brain/bugs/` как починенный.
