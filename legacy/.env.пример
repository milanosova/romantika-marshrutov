# Скопировать в .env и заполнить:  cp .env.пример .env && chmod 600 .env
# .env в git не попадает — он в .gitignore.

# BotFather → /token → @romantika_marshrutov_bot
TOKEN=

# Telegram id владельца. Узнать: написать боту /whoami
ADMIN_ID=355363829
